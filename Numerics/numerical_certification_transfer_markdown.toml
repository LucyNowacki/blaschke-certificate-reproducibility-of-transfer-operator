schema_version = "1.0.0"
chapter_label = "chap:numerical-certification-transfer"
chapter_source = "chapters/numerical_certification_transfer.tex"
chapter_source_sha256_at_mapping = "de540dc4ed905a42b29c783cfae890b6dc02545297b712e08e32c28c0b47902c"
direct_theory_source = "chapters/single_space_legendre_edmd.tex"
direct_theory_source_sha256_at_mapping = "0c0ae3c0278bba2b291da6921bb32773cf443f7646eb18bc03a84ea529448f4c"
mapping_date = "2026-08-29"
mapping_scope = "52 stable Markdown owners covering the complete 68-code-cell execution topology"

[fidelity_replacements."md-12580490"]
old = '''Their certified intersection equals the branchwise upper endpoint on every deployed boundary cell, so the selected unresolved-input certificate demonstrates no cancellation gain.'''
new = '''On the deployed boundary cover, the direct coherent interval enclosure was not smaller than the branchwise enclosure, so the certified minimum selected the branchwise upper endpoint.  This compares two rigorous upper enclosures only; equality of their endpoints does not prove that the unknown exact coherent tail has no cancellation.'''

[fidelity_replacements."e7c6be15"]
old = '''The diagnostic is visual only: it shows the shape of the finite-section moat near $\mu^2$, while the certification table uses the sampled minimum and small-gain quantities computed earlier.'''
new = '''The diagnostic is visual only: it shows the shape of the finite-section moat near $\mu^2$.  The later certification table instead uses a complete-circle Schur or Laurent lower bound, the exact-dyadic matrix-replacement radius, the silent-zero distance, and the certified perturbation radius.'''

[entries."67a01ec3"]
code_cell_ids = ["6a823024", "code-bffea704"]
chapter_labels = ["subsec:numerics-certification-provenance", "subsec:numerics-three-gauges"]
evidence_status = ["infrastructure_only"]
heading_from = "## Mathematics for Cell 3"
heading_to = "## Mathematics for setup Cells 5N and 6N"
mathematics = '''The proof is carried out on the fixed space $X=H^2(E_r)$, so precision, worker count, and output roots are part of the reproducible numerical experiment, not free mathematical parameters.  These cells create the environment in which later bounds are evaluated, but no inequality such as $\|\mathcal L-\mathcal L_{N,M}^X\|\leq\widehat\varepsilon_{N,M}$ follows from directory creation or decimal precision alone.  Intuitively, they fix the ruler and filing system; the later Arb gates establish what that ruler proves.'''

[entries."18374041"]
code_cell_ids = ["91795b99"]
chapter_labels = ["subsec:numerics-branch-geometry", "subsec:numerics-phase2-schur"]
evidence_status = ["infrastructure_only"]
heading_from = "## Mathematics for Cell 4"
heading_to = "## Mathematics for configuration Cell 8N"
mathematics = '''The admissible geometry must satisfy $1<r_\tau(\rho)<r<\rho$.  It yields the two contraction ratios $q_{\mathrm{out}}=r/\rho$ and $q_{\mathrm{gap}}=r_\tau(\rho)/r$, with endpoint reference $q_*=\max\{q_{\mathrm{out}},q_{\mathrm{gap}}\}<1$.  The configuration records the proposed values and target inventory.  Certification occurs only when later complete-boundary calculations prove the inclusions and all component gates; the configured decimal values do not prove themselves.'''

[entries."inline-helper-bootstrap-heading"]
code_cell_ids = ["inline-helper-bootstrap-code"]
chapter_labels = ["subsec:numerics-certification-provenance"]
evidence_status = ["infrastructure_only"]
mathematics = '''For every displayed helper source $s$, the loader checks a declared digest $h=\operatorname{SHA256}(s)$ before installing the module used by the kernel and its process workers.  The equality of digests proves source identity between the visible cell and executed helper copy; it is a provenance statement, not a numerical theorem.  The mathematical status of a helper still comes from the complete-set bounds, strict inequalities, and fail-closed gates implemented by that source.'''

[entries."inline-helper-heading-1"]
code_cell_ids = ["inline-helper-source-1", "43ecd634"]
chapter_labels = ["subsec:numerics-legendre-gauss-transfer-block", "eq:numerics-transfer-block-entry", "eq:numerics-Lhat-PWh"]
evidence_status = ["infrastructure_only", "empirical_diagnostic"]
mathematics = '''The worker evaluates the Perron--Frobenius block
$$
(\widehat L_N^{(M)})_{k\ell}=\sum_{j=1}^M w_j\overline{\widetilde P_k(x_j)}\sum_b\phi_b(x_j)\widetilde P_\ell(\tau_b(x_j))
$$
or, in matrix form, $\widehat L_N^{(M)}=P^*Wh$.  This is the finite transfer operator, not a Koopman matrix.  The helper also diagonalises raw blocks and greedily matches eigenvalues to external targets; those matches measure empirical proximity only and do not supply Riesz ranks or complete-contour moats.'''

[entries."inline-helper-heading-2"]
code_cell_ids = ["inline-helper-source-2", "0e01d379"]
chapter_labels = ["subsec:numerics-certification-provenance", "subsec:numerics-cluster-matching"]
evidence_status = ["infrastructure_only", "empirical_diagnostic"]
mathematics = '''This map-generic bridge keeps distinct the external target list, sampled diagnostics, certified analytic geometry, deterministic operator radius, and contour certificate.  A raw number $\Delta_j=\max_{z\in Z_j}|z-\lambda_j|$ is therefore never promoted merely because it is small.  The shared utilities persist and display values, while a theorem-facing claim requires the later chain $\widehat\varepsilon<\underline s_{\mathrm{lift}}$ together with an independently certified finite count.'''

[entries."math-cell-8-blaschke-data"]
code_cell_ids = ["d09d9963"]
chapter_labels = ["subsec:numerics-branch-geometry", "eq:numerics-transfer-inverse-branch", "subsec:numerics-phase1-empirical"]
evidence_status = ["infrastructure_only"]
mathematics = '''The source instantiates $(\mathcal Lf)(x)=\sum_b\phi_b(x)f(\tau_b(x))$ for the symmetric parameter $\mu=0.3$ and sets $\alpha=(1+\mu)/2$.  The external Blaschke formula supplies the names $1$, $\alpha^n$, and $\mu^n$ and their expected multiplicities.  Intuitively, these are addresses for proposed spectral packets.  They do not count eigenvalues of the computed matrix or certify any exact-operator packet; those facts are obtained independently in the complete-contour stage.'''

[entries."math-cell-9-branch-geometry"]
code_cell_ids = ["ce7ba80a"]
chapter_labels = ["subsec:numerics-branch-geometry", "subsec:numerics-phase1-empirical"]
evidence_status = ["empirical_diagnostic"]
heading_from = "## Mathematics for Cell 9"
heading_to = "## Mathematics for sampled branch-geometry Cell 21N"
mathematics = '''The grid evaluates the Joukowski boundary $\Gamma_\rho(\theta)=(\rho e^{i\theta}+\rho^{-1}e^{-i\theta})/2$ and estimates $\max_{b,\theta}r_B(\tau_b(\Gamma_\rho(\theta)))$.  It also compares $\Phi(\rho)=\sum_b\sup|\phi_b|$ with the coherent profile $\Phi_*(\rho)=\sup_\omega\sum_b|\phi_b(\omega)|$.  A finite angular maximum can miss the true supremum, so this cell explains and tests the geometry; the certified inclusion $\tau_b(\overline E_\rho)\subset E_{r_\tau}$ is produced later by a complete Arb cover.'''

[entries."phase1-math-11"]
code_cell_ids = ["a5385979"]
chapter_labels = ["subsec:numerics-legendre-gauss-transfer-block", "subsec:numerics-cluster-matching", "subsec:numerics-phase1-empirical"]
evidence_status = ["empirical_diagnostic"]
heading_from = "## Phase 1 mathematics for Cell 11"
heading_to = "## Phase 1 mathematics for execution Cell 24N"
mathematics = '''For each square pair $M=N$, the cell assembles $\widehat L_N^{(N)}$, computes its finite spectrum, and removes matched eigenvalues from a mutable multiset.  A target of multiplicity $m_j$ receives its $m_j$ nearest unused eigenvalues and the reported error is $\Delta_j(N,N)=\max_{z\in Z_j}|z-\lambda_j|$.  This order-dependent construction is useful reconnaissance.  It is not a Hausdorff spectral bound, an operator-norm estimate, or a proof of the target multiplicity.'''

[entries."phase1-math-12"]
code_cell_ids = ["2a6f973c"]
chapter_labels = ["subsec:numerics-cluster-matching", "subsec:numerics-phase1-empirical", "fig:numerics-phase1-raw-cluster-errors"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
heading_from = "## Phase 1 mathematics for Cell 12"
heading_to = "## Phase 1 mathematics for execution Cell 26N"
mathematics = '''The displayed guide fits $\log\Delta_j(N,N)\approx a+bN$ over the first eight ordered nontrivial packet rows selected by the live code, including both $\alpha$ and $\mu$ families.  Its base $q_{\mathrm{fit}}=e^b$ is empirical.  The separate $q_*$ line is an endpoint geometric reference: the proved asymptotic statement is available for every weakened base $q_0\in(q_*,1)$, not automatically at $q_*$ itself, and neither curve is a finite-$N$ operator certificate.'''

[entries."phase1-math-14"]
code_cell_ids = ["c1b4b6e4"]
chapter_labels = ["subsec:numerics-phase1-empirical", "fig:numerics-phase1-heatmap-raw", "fig:numerics-phase1-heatmap-excess"]
evidence_status = ["infrastructure_only", "empirical_diagnostic"]
heading_from = "## Phase 1 mathematics for Cell 14"
heading_to = "## Phase 1 mathematics for execution Cell 29N"
mathematics = '''The heatmap transformations study finite quadrature sensitivity through $m=M-N$ and the ratio $R_j(N,M)=\Delta_j(N,M)/\Delta_j(N,M_{\max})$.  Values of $\log_{10}R_j$ near zero suggest saturation against the chosen finite reference row, while the local slope in $M$ describes only the sampled schedule.  The theorem-facing statement that collocation is negligible at $N=600,M=610$ comes instead from its separately certified matrix contribution, not from these heatmaps.'''

[entries."inline-helper-heading-17"]
code_cell_ids = ["inline-helper-source-17", "producer-phase1-diagnostics", "cc338a81"]
chapter_labels = ["subsec:numerics-phase1-empirical", "fig:numerics-phase1-M-sweep", "fig:numerics-phase1-leading-eigencloud", "fig:numerics-phase1-oversampling-displacement"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
mathematics = '''The producer reconstructs every retained Phase 1 table from the locked map, the explicit $(N,M)$ schedules, and the same high-precision block assembler; destination CSV files are not accepted as seeds.  It then displays finite-$M$ curves, heatmaps, eigencloud matches, and displacements.  These are source-reproducible evaluations of $\operatorname{spec}(\widehat L_N^{(M)})$, but none enters the deterministic radius, a complete-contour moat, or a Riesz-rank gate.'''

[entries."822c8f7f"]
code_cell_ids = ["4d7a9701"]
chapter_labels = ["subsec:numerics-certification-provenance"]
evidence_status = ["infrastructure_only"]
heading_from = "## Mathematics for Cell 17"
heading_to = "## Mathematics for precision-display Cell 33N"
mathematics = '''The displayed decimal precision is the working precision at which later high-precision and interval producers are invoked.  More digits can reduce rounding uncertainty, but the integer $\mathrm{dps}$ is not itself an error bound.  Certification requires outward-rounded enclosures and strict gates such as $\widehat\varepsilon<\underline s_{\mathrm{lift}}$; this cell merely makes the active arithmetic setting auditable.'''

[entries."5d43e2ec"]
code_cell_ids = ["9b8c4f29"]
chapter_labels = ["subsec:numerics-three-gauges", "eq:numerics-coordinate-chain", "eq:numerics-pure-r-gauge-matrix", "eq:numerics-X-gauge-matrix"]
evidence_status = ["empirical_diagnostic"]
heading_from = "## Mathematics for Cell 18"
heading_to = "## Mathematics for finite coordinate Cell 35N"
mathematics = '''For one $f\in V_N$, ordinary Legendre coefficients $c$ become pure-scaled coefficients $y=D_{r,N}c$ and Hardy packet coordinates $d=T_N(r)y$.  The synthesis map is chosen so that $\|f\|_X=\|d\|_2$.  Hence
$$
L_N^{(r)}=D_{r,N}\widehat L_ND_{r,N}^{-1},\qquad A_N^X=T_N(r)L_N^{(r)}T_N(r)^{-1}.
$$
The matrices are similar and share eigenvalues, but their singular values have different norm meanings.  This floating-point cell illustrates the transform; it is not the validated $N=600$ transport certificate.'''

[entries."inline-helper-heading-6"]
code_cell_ids = ["inline-helper-source-6"]
chapter_labels = ["subsec:numerics-branch-geometry", "subsec:numerics-phase2-schur"]
evidence_status = ["certified_input_producer"]
mathematics = '''The geometry helper covers the complete ellipse boundary with complex Arb cells and proves the branch-image nesting $\tau_b(\overline E_\rho)\subset E_{r_\tau}\Subset E_r\Subset E_\rho$.  On the same cover it encloses the transfer weights used by the tail rows.  Unlike a plotted angular grid, interval images cover every boundary point, so the resulting $r_\tau$ and weight upper bounds may feed the deterministic theorem gate.'''

[entries."inline-helper-heading-7"]
code_cell_ids = ["inline-helper-source-7"]
chapter_labels = ["subsec:numerics-three-gauges", "eq:numerics-X-gauge-matrix"]
evidence_status = ["certified_input_producer", "certified_component"]
mathematics = '''For every finite matrix $B$, the Hardy-coordinate transform obeys
$$
\|T_NBT_N^{-1}\|_2\leq\|T_N\|_2\,\|T_N^{-1}\|_2\,\|B\|_2\leq\widehat\kappa_N^{X\leftrightarrow r}\|B\|_2.
$$
The helper certifies an upper bound for the condition number; it does not assert equality with the true amplification.  This factor transports the pure $r$-scaled collocation defect into the Euclidean norm that is isometric to $X$.'''

[entries."inline-helper-heading-8"]
code_cell_ids = ["inline-helper-source-8"]
chapter_labels = ["subsec:numerics-schur-envelope", "subsec:numerics-phase2-schur"]
evidence_status = ["certified_input_producer"]
mathematics = '''The matrix helper reconstructs the ordinary and starred separable-Frobenius candidates in the pure $r$-scaled gauge, where degree decay separates into row and column factors.  At this stage the row still uses the legacy $D_M=1$ convention and explicitly records `finite_M_certified = False`; it is therefore a theorem-input candidate, not yet a certified finite-$M$ component.  Cell 49N later applies $D_{\mathrm{safe}}=C_{\mathrm{GL}}^{\mathrm{safe}}/C_{\mathrm{GL}}$, selects the safe minimum, and only then permits Hardy-gauge transport into the final radius.'''

[entries."inline-helper-heading-9"]
code_cell_ids = ["inline-helper-source-9"]
chapter_labels = ["subsec:numerics-schur-envelope", "eq:numerics-single-space-envelope", "subsec:numerics-phase2-schur"]
evidence_status = ["certified_input_producer"]
mathematics = '''The clean-room orchestrator joins branch geometry, coordinate transport, and the pure-scaled matrix row from named source products.  Its target is the structure
$$
\widehat\varepsilon=\sqrt{(\widehat B_{\mathrm{out}}^X)^2+(\widehat B_{\mathrm{in}}^X)^2}+\widehat\kappa_N^{X\leftrightarrow r}\widehat B_{N,M}^{\mathrm{mat,best}}.
$$
It fails closed when a required component or provenance gate is absent; a small numerical value cannot substitute for the required logical conjunction.'''

[entries."inline-helper-heading-10"]
code_cell_ids = ["inline-helper-source-10"]
chapter_labels = ["subsec:numerics-phase2-schur", "subsec:numerics-bottleneck-future"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
mathematics = '''This helper reconstructs old whole-ellipse, branch-image, response-prefactor, and wide-radius comparison rows from explicit source inputs.  All rows have the visual decomposition $\varepsilon_s=\sqrt{O_s^2+I_s^2}+C_s$, but historical or alternative rows are not automatically promoted.  They explain which mechanism dominates and how the design changed; the theorem consumes only the balanced row whose current component gates all pass.'''

[entries."498d4a92"]
code_cell_ids = ["6451c7fe"]
chapter_labels = ["subsec:numerics-schur-envelope", "eq:numerics-single-space-envelope", "subsec:numerics-phase2-schur"]
evidence_status = ["certified_input_producer"]
heading_from = "## Mathematics for Cell 19"
heading_to = "## Phase 2 mathematics for clean-room execution Cell 37N"
mathematics = '''Execution Cell 37N regenerates the theorem-facing geometry, transport, and matrix inputs rather than accepting their destination files as evidence.  The analytic part uses a root-sum-square because the resolved and unresolved input components are orthogonal in $X$ before their two operator bounds are combined; the collocation defect is then added by the triangle inequality.  The result is a candidate for $\|\mathcal L-\mathcal L_{N,M}^X\|$, but final promotion still awaits finite-$M$, resolved-response, unresolved-input, and aggregation gates.'''

[entries."e6ab3425"]
code_cell_ids = ["44cb0b52"]
chapter_labels = ["subsec:numerics-branch-geometry", "subsec:numerics-phase2-schur"]
evidence_status = ["empirical_diagnostic"]
heading_from = "## Phase 2 mathematics for Cell 20"
heading_to = "## Phase 2 mathematics for sampled geometry Cell 39N"
mathematics = '''At sampled points $\omega_j\in\partial E_\rho$, the cell records $r_{\tau,b}^{\mathrm{sample}}=\max_j r_B(\tau_b(\omega_j))$ and $\Phi_b^{\mathrm{sample}}=\max_j|\phi_b(\omega_j)|$.  The comparison reveals which branch gap is active and checks that sampled images lie inside the certified envelope.  Because $\max_j$ over a finite grid may be strictly smaller than $\sup_{\partial E_\rho}$, these rows audit the later complete-boundary proof but never replace it.'''

[entries."60ccec05"]
code_cell_ids = ["e40904af"]
chapter_labels = ["subsec:numerics-branch-geometry", "fig:numerics-phase2-certified-geometry-ellipses"]
evidence_status = ["presentation_only", "empirical_diagnostic"]
heading_from = "## Phase 2 mathematics for Cell 21"
heading_to = "## Phase 2 mathematics for geometry plot Cell 41N"
mathematics = '''The nested picture represents $\tau_b(\partial E_\rho)$ inside $E_{r_\tau}\Subset E_r\Subset E_\rho$.  Moving from the outer ellipse to the inner branch image explains the decay factor $q_{\mathrm{gap}}=r_\tau/r$: unresolved high modes are evaluated where the inverse branches land, not everywhere in $E_r$.  The curves are sampled and therefore explanatory; the solid theorem edge in the dependency graph comes from the interval geometry certificate consumed here.'''

[entries."3f36d6e7"]
code_cell_ids = ["0443774e"]
chapter_labels = ["subsec:numerics-schur-envelope", "subsec:numerics-phase2-schur", "legEDMD:def-branch-image-Chebyshev-input-tail-kernel", "eq:branch-image-Chebyshev-tail-kernel", "legEDMD:rem-certified-finite-prefix-pointwise-kernel"]
evidence_status = ["empirical_diagnostic"]
heading_from = "## Phase 2 mathematics for Cell 22"
heading_to = "## Phase 2 mathematics for sampled tail-kernel Cell 43N"
mathematics = '''The exact unresolved packet tail is $K_N^{(r)}(w)=(\sum_{m\geq N}|\Psi_m^{(r)}(w)|^2)^{1/2}$.  The code evaluates a conservative closed-form majorant and forms the coherent branch-image profile $\sum_b|\phi_b(\omega)|K_{N,\mathrm{upper}}^{(r)}(\tau_b(\omega))$ on a finite grid.  Pointwise evaluation makes the geometric improvement intuitive, but the theorem-facing input bound is the outward-rounded supremum over a complete Arb boundary cover.'''

[entries."b5148f42"]
code_cell_ids = ["9f2bf1b1"]
chapter_labels = ["subsec:numerics-schur-envelope", "subsec:numerics-phase2-schur"]
evidence_status = ["empirical_diagnostic"]
heading_from = "## Phase 2 mathematics for Cell 23"
heading_to = "## Phase 2 mathematics for sampled tail comparison Cell 45N"
mathematics = '''Three upper profiles are compared: one common enclosing ellipse, separate branchwise ellipses, and direct evaluation on sampled branch images.  Ratios below one quantify how much the common ellipse overestimates this particular finite-grid profile.  They do not prove the exact coherent tail or its cancellation: each numerator and denominator is an upper construction, and only the later complete-boundary interval row can enter $\widehat B_{\mathrm{in}}^X$.'''

[entries."16dbd2e8"]
code_cell_ids = ["b4390377"]
chapter_labels = ["subsec:numerics-phase2-schur"]
evidence_status = ["presentation_only", "empirical_diagnostic"]
heading_from = "## Phase 2 mathematics for Cell 24"
heading_to = "## Phase 2 mathematics for tail-ratio plot Cell 47N"
mathematics = '''The plotted ratios have the form $R(N)=B_{\mathrm{image}}(N)/B_{\mathrm{ellipse}}(N)$.  A value $R(N)<1$ visualises the benefit of retaining pointwise branch geometry, while the line $R=1$ is only a comparison baseline.  The curves inherit sampled inputs and therefore remain diagnostics; they do not certify an operator error, a contour moat, or the absence of exact branch cancellation.'''

[entries."inline-helper-heading-14"]
code_cell_ids = ["inline-helper-source-14", "fm24ac"]
chapter_labels = ["eq:numerics-safe-GL-remainder", "eq:numerics-safe-GL-prefactor", "eq:numerics-safe-effective-multiplier", "eq:numerics-safe-Bmat-rescaling"]
evidence_status = ["certified_input_producer", "certified_component"]
mathematics = '''Gauss--Legendre exactness through degree $2M-1$ leaves only the Chebyshev tail, giving
$$
|R_M[f]|\leq\frac{8\rho}{\rho-1}\rho^{-2M}\|f\|_{\mathcal A(E_\rho)}.
$$
The helper promotes the legacy $D_M=1$ matrix row by $D_{\mathrm{safe}}=C_{\mathrm{GL}}^{\mathrm{safe}}/C_{\mathrm{GL}}$ and then transports it into the Hardy gauge.  Increasing $M$ suppresses this collocation term through $\rho^{-2M}$; it does not remove the unresolved analytic input tail controlled by $N$ and the branch gap.'''

[entries."inline-helper-heading-3"]
code_cell_ids = ["inline-helper-source-3"]
chapter_labels = ["subsec:numerics-schur-envelope", "eq:numerics-single-space-envelope", "subsec:numerics-phase2-schur"]
evidence_status = ["certified_input_producer", "certified_component"]
mathematics = '''The unresolved-input row starts at mode $N$,
$$
B_{\mathrm{in}}=\sup_{\omega\in\partial E_\rho}\left(\sum_{m=N}^{\infty}\left|\sum_b\phi_b(\omega)\Psi_m^{(r)}(\tau_b(\omega))\right|^2\right)^{1/2},
$$
whereas the resolved response uses modes $0\leq m<N$.  The helper encloses coherent prefixes and rigorous remainders and also computes independent branchwise fallbacks.  A minimum is permitted only among certified upper bounds for the same quantity.  Equal upper endpoints do not identify the unknown exact norm or prove absence of cancellation.'''

[entries."inline-helper-heading-15"]
code_cell_ids = ["inline-helper-source-15", "resp24ac"]
chapter_labels = ["subsec:numerics-schur-envelope", "subsec:numerics-phase2-schur", "eq:numerics-phase2-single-space-envelope"]
evidence_status = ["certified_input_producer", "certified_component"]
mathematics = '''For resolved packet rows $q_m^{\mathrm{res}}(\omega)=\sum_b\phi_b(\omega)\Psi_m^{(r)}(\tau_b(\omega))$, the response prefactor is
$$
C_{N,\mathrm{resp},*}^X=\sup_{\partial E_\rho}\left(\sum_{m=0}^{N-1}|q_m^{\mathrm{res}}(\omega)|^2\right)^{1/2}.
$$
It is a norm amplification, not an error by itself.  Multiplication by the certified output tail gives $B_{\mathrm{out}}=C_{N,\mathrm{resp},*}^X Z_{N,\mathrm{tail}}^{\mathrm{out}}$.  Cell 54N selects the smallest compatible certified candidate cellwise, then takes the complete-boundary maximum.'''

[entries."inline-helper-heading-16"]
code_cell_ids = ["inline-helper-source-16", "code-2ec30a67", "code-7598db69", "code-7a22b58f"]
chapter_labels = ["eq:numerics-single-space-envelope", "eq:numerics-phase2-single-space-envelope", "eq:numerics-promoted-final-epsilon", "subsec:numerics-phase2-schur", "fig:numerics-phase2-certificate-array"]
evidence_status = ["certified_input_producer", "certified_component", "presentation_only", "validation_test"]
mathematics = '''Write $f=f_r+f_u$ with $f_r=P_N^Xf$ and $f_u=(I-P_N^X)f$.  Their $X$-orthogonality gives
$$
B_{\mathrm{out}}\|f_r\|_X+B_{\mathrm{in}}\|f_u\|_X\leq\sqrt{B_{\mathrm{out}}^2+B_{\mathrm{in}}^2}\,\|f\|_X.
$$
Thus Cell 55N forms $\widehat\varepsilon=\sqrt{B_{\mathrm{out}}^2+B_{\mathrm{in}}^2}+\widehat\kappa\widehat B_{\mathrm{mat,best}}$.  The root-sum-square uses input-space orthogonality; it does not assert cancellation of output errors.  Cell 56N presents certified input rows and Cell 57N checks identities without replacing the component proofs.'''

[entries."b03841e6"]
code_cell_ids = ["06fef5b3"]
chapter_labels = ["subsec:numerics-schur-envelope", "subsec:numerics-phase2-schur"]
evidence_status = ["presentation_only"]
heading_from = "## Phase 2 mathematics for Cell 25"
heading_to = "## Phase 2 mathematics for response summary Cell 59N"
mathematics = '''This table compares three certified upper enclosures of the same resolved-response norm and records which candidate is smallest on each boundary cell.  The mathematical quantity remains $C_{N,\mathrm{resp},*}^X$, and the output leakage remains $B_{\mathrm{out}}=C_{N,\mathrm{resp},*}^X Z_{N,\mathrm{tail}}^{\mathrm{out}}$.  Displaying the candidates and their selection provenance makes the minimisation auditable; it performs no new enclosure and cannot strengthen the status established by Cells 52N--54N.'''

[entries."e75453d7"]
code_cell_ids = ["c92b9a83"]
chapter_labels = ["subsec:numerics-phase2-schur", "fig:numerics-phase2-certificate-components"]
evidence_status = ["presentation_only"]
heading_from = "## Phase 2 mathematics for Cell 26"
heading_to = "## Phase 2 mathematics for component plots Cell 61N"
mathematics = '''Each bar visualises one term of $\varepsilon_s=\sqrt{O_s^2+I_s^2}+C_s$.  The hierarchy $I_s\gg O_s\gg C_s$ identifies the unresolved input tail as the active bottleneck for the promoted configuration and shows why extra quadrature points cannot materially improve the final radius.  The plot consumes already classified rows; it neither recomputes interval bounds nor promotes historical comparison rows into theorem evidence.'''

[entries."282ee430"]
code_cell_ids = ["00e8904c"]
chapter_labels = ["eq:numerics-promoted-final-epsilon", "subsec:numerics-phase2-schur", "subsec:numerics-certification-provenance"]
evidence_status = ["presentation_only"]
heading_from = "## Phase 2 mathematics for Cell 27"
heading_to = "## Phase 2 mathematics for export Cell 63N"
mathematics = '''Exporting serialises the promoted row without recomputing it.  The theorem-facing value is the upper enclosure $\widehat\varepsilon_{600,610}^{X,\mathrm{fin,best}}\leq3.326443383901743\times10^{-20}$, and `total_certified` is the conjunction of geometry, response, input-tail, transport, matrix, and finite-$M$ gates.  Candidate equality is reported as equality of upper endpoints only; it does not establish equality of exact norms or rule out unknown coherent cancellation.'''

[entries."96176817"]
code_cell_ids = ["7d75c6ff"]
chapter_labels = ["subsec:numerics-phase3-rates", "eq:numerics-promoted-final-epsilon"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
heading_from = "## Mathematics for Cell 30"
heading_to = "## Phase 3 mathematics for ingestion Cell 67N"
mathematics = '''Phase 3 joins empirical packet errors to the already certified $N=600,M=610$ Phase 2 row; it creates no new certificate.  The finite normalisations $\widehat\varepsilon/q_*^{600}$ and $\widehat\varepsilon^{1/600}$ summarise one certified point.  They do not prove a uniform constant or an endpoint rate.  The theorem only guarantees exponential bounds for every $q_0\in(q_*,1)$ under the stated sampling and uniform-transport hypotheses.'''

[entries."f23c633b"]
code_cell_ids = ["df668eaa"]
chapter_labels = ["subsec:numerics-phase3-rates", "tab:numerics-phase3-branch-image-fitted-bases"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
heading_from = "## Mathematics for Cell 31"
heading_to = "## Phase 3 mathematics for fitted-rate Cell 69N"
mathematics = '''The live fit retains rows satisfying $10^{-14}<\Delta_j(N,N)<10^{-1}$ and estimates $\log\Delta_j\approx a_j+b_jN$, so $q_{\mathrm{fit},j}=e^{b_j}$.  Most displayed packets have only two or three usable points.  These short-range slopes describe selected raw finite-section errors; they are neither operator-norm bounds nor asymptotic convergence theorems, and they do not certify packet multiplicities.'''

[entries."ec07075c"]
code_cell_ids = ["5cb2c38a"]
chapter_labels = ["subsec:numerics-phase3-rates", "fig:numerics-phase3-deterministic-components-rate", "eq:numerics-phase2-single-space-envelope"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
heading_from = "## Mathematics for Cell 32"
heading_to = "## Phase 3 mathematics for certificate-comparison Cell 71N"
mathematics = '''The comparison uses the exact promoted notation $C_s=\widehat\kappa_N^{X\leftrightarrow r}\widehat B_{N,M,s}^{\mathrm{mat,best}}$ and displays $\varepsilon_s/q_{*,s}^{N_s}$ and $\varepsilon_s^{1/N_s}$.  These dimension-normalised quantities help distinguish a large finite prefactor from a different geometric core.  Only a row whose complete certification flags pass is theorem-facing; plotting old, branch-image, response, and wide rows together does not give them equal status.'''

[entries."2d22bd26"]
code_cell_ids = ["e4cd9776"]
chapter_labels = ["subsec:numerics-phase3-rates", "fig:numerics-phase3-branch-image-bridge", "eq:numerics-promoted-final-epsilon"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
heading_from = "## Mathematics for Cell 33"
heading_to = "## Phase 3 mathematics for bridge-plot Cell 73N"
mathematics = '''The left panel contains greedy packet errors $\Delta_j(N,N)$; the theorem-facing point in the right panel is the independent operator bound $\|\mathcal L-\mathcal L_{600,610}^X\|\leq\widehat\varepsilon_{600,610}$.  Its horizontal geometric guide is specifically $q_*^{600}$, not an assertion that $q_*^N$ bounds every dimension.  Juxtaposition tests qualitative consistency while preserving the different logical status of raw spectra and deterministic certification.'''

[entries."c6b741e1"]
code_cell_ids = ["3410c676"]
chapter_labels = ["subsec:numerics-phase3-rates", "fig:numerics-phase3-deterministic-components-rate", "subsec:numerics-bottleneck-future"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
heading_from = "## Mathematics for Cell 34"
heading_to = "## Phase 3 mathematics for extrapolation Cell 75N"
mathematics = '''The curves extend the single promoted row by the heuristic cores $q_{\mathrm{out}}^N$, $q_{\mathrm{gap}}^N$, and $q_{\mathrm{out}}^N\sqrt{2N-1}$.  They show how each mechanism would scale if the finite prefactors remained fixed.  Only the starred $N=600,M=610$ point is certified; every other plotted point is a geometric extrapolation and must not be used as an operator bound or a contour small-gain input.'''

[entries."inline-helper-heading-11"]
code_cell_ids = ["inline-helper-source-11"]
chapter_labels = ["subsec:numerics-phase4-contours", "fig:numerics-phase4-mu2-sampled-landscape"]
evidence_status = ["empirical_diagnostic"]
mathematics = '''The worker evaluates the finite-grid surface $S(x,y)=\sigma_{\min}((x+iy)I-A_N^X)$ in independent process blocks.  It supports the local and global visual landscapes by recombining rows with deterministic coordinates.  Even when computed at high resolution, a surface sample does not cover every point of a contour; it can reveal where a moat may be narrow, but it cannot provide the positive complete-circle lower bound required by the Riesz argument.'''

[entries."inline-helper-heading-12"]
code_cell_ids = ["inline-helper-source-12"]
chapter_labels = ["subsec:numerics-phase4-contours", "fig:numerics-phase4-global-sampled-landscape"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
mathematics = '''This producer reconstructs historical Phase 4 profiles, surfaces, and plotting inputs from source-controlled schedules and finite matrices rather than accepting destination images as seeds.  The mathematical object is still the sampled pencil $zI-A_N^X$.  Source reconstruction strengthens provenance of the pictures, not their proof status: sampled minima, dashboards, and landscapes remain excluded from count, moat, and small-gain theorem gates.'''

[entries."a81aa734"]
code_cell_ids = ["59a1c39c"]
chapter_labels = ["subsec:numerics-phase4-contours", "eq:numerics-small-gain"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
mathematics = '''For a sampled angle set $\Theta_J$, the displayed quantity is $s_J=\min_{\theta\in\Theta_J}\sigma_{\min}((c+\delta e^{i\theta})I-A_N^X)$.  Because $\Theta_J$ is only a subset of the circle,
$$
\inf_{0\leq\theta<2\pi}\sigma_{\min}((c+\delta e^{i\theta})I-A_N^X)\leq s_J.
$$
Thus the sampled minimum is an upper estimate of the true moat, not a certified lower bound.  The first-fifteen figures are retained reconnaissance only.'''

[entries."3d797d1d"]
code_cell_ids = ["32961420"]
chapter_labels = ["subsec:numerics-phase4-contours", "eq:numerics-small-gain"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
mathematics = '''Both packet profiles plot $\theta\mapsto\sigma_{\min}((c+\delta e^{i\theta})I-A_N^X)$, including the restored $\alpha^{11}$ comparison.  Markers identify the smallest sampled ordinate and the deterministic radius scale.  A plotted ratio $\widehat\varepsilon/s_J<1$ is only a sampled indication, since unsampled angles could lower the moat.  The final small-gain claim uses a complete-circle Schur or Laurent lower bound after all replacement radii are transported and subtracted.'''

[entries."b8581535"]
code_cell_ids = ["phase2-math-41"]
chapter_labels = ["subsec:numerics-phase4-contours", "eq:numerics-small-gain"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
mathematics = '''The comparison changes angular resolution for fragile packets and observes how the discrete minimum $s_J$ moves.  Stability across two grids is useful evidence that a proposed contour is sensible, while movement exposes under-resolution.  Neither outcome proves the continuum infimum: for every finite $J$, $s_J$ can still exceed $\inf_{z\in\Gamma}\sigma_{\min}(zI-A_N^X)$.  The cell therefore informs contour design but does not certify it.'''

[entries."e7c6be15"]
code_cell_ids = ["340f0292"]
chapter_labels = ["subsec:numerics-phase4-contours", "fig:numerics-phase4-mu2-sampled-landscape"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
heading_from = "## Mathematics for Cell 81"
heading_to = "## Phase 4 mathematics for local landscape Cell 84N"
mathematics = '''The height $Z(x,y)=\log_{10}\max\{\sigma_{\min}((x+iy)I-A_N^X),S_{\mathrm{floor}}\}$ visualises pseudospectral valleys around the proposed $\mu^2$ circle.  Low valleys mean the resolvent $\|(zI-A_N^X)^{-1}\|_2=1/\sigma_{\min}(zI-A_N^X)$ is large.  This geometric intuition is valuable, but the rectangular grid and interpolated circle do not establish a uniform lower bound; the validated table later uses complete-circle arithmetic.'''

[entries."fc892fd7"]
code_cell_ids = ["add52fcf"]
chapter_labels = ["subsec:numerics-phase4-contours", "fig:numerics-phase4-global-sampled-landscape"]
evidence_status = ["empirical_diagnostic", "presentation_only"]
heading_from = "## Mathematics for Cell 82"
heading_to = "## Phase 4 mathematics for global landscape Cell 86N"
mathematics = '''All plotted packets lie on one shared surface $z\mapsto\log_{10}\sigma_{\min}(zI-A_N^X)$ because they use the same finite Hardy matrix.  Overlaying $\Gamma_j(\theta)=c_j+\delta_je^{i\theta}$ shows how different spectral packets sit in that resolvent geometry.  The global grid helps detect contour collisions and near-zero fragility, but it remains a sampled map; it does not prove any of the twenty-four complete-circle moats or finite counts.'''

[entries."5b79cbd6"]
code_cell_ids = ["8bf58bd5"]
chapter_labels = ["subsec:numerics-phase4-contours", "fig:numerics-phase4-global-sampled-landscape"]
evidence_status = ["presentation_only", "empirical_diagnostic"]
heading_from = "## Mathematics for Cell 82B"
heading_to = "## Phase 4 mathematics for alternative-view Cell 88N"
mathematics = '''The zoom restricts the domain while preserving Euclidean geometry; the symlog view changes only the displayed horizontal coordinate.  Neither transformation changes the underlying values $\sigma_{\min}(zI-A_N^X)$.  In particular, circles need not look circular after symlog scaling, so distances must be interpreted in the original complex plane.  These are readability transforms of diagnostic data and cannot modify moat, count, or small-gain status.'''

[entries."59737fdb1e5a"]
code_cell_ids = ["ea23bc60"]
chapter_labels = ["subsec:numerics-phase4-contours", "fig:numerics-phase4-global-sampled-landscape"]
evidence_status = ["presentation_only"]
mathematics = '''The cell displays a stored raster whose pixels encode the previously computed sampled field $Z(x,y)=\log_{10}\sigma_{\min}((x+iy)I-A_N^X)$.  It performs no matrix evaluation, interval enclosure, or contour optimisation.  Consequently its mathematical content is inherited entirely from the diagnostic producer and its proof status cannot exceed that source; visual resolution is not numerical certification.'''

[entries."1c203394"]
code_cell_ids = ["009356fa"]
chapter_labels = ["subsec:numerics-phase4-contours", "fig:numerics-phase4-global-sampled-landscape"]
evidence_status = ["presentation_only", "empirical_diagnostic"]
mathematics = '''The interactive dashboard queries retained rows of the sampled Hardy-pencil surface and overlays finite eigenvalues and candidate contours.  Moving or selecting a packet changes the view, not the operator or the data.  Its values approximate $\sigma_{\min}(zI-A_N^X)$ at stored grid points, so the dashboard is a tool for geometric understanding and audit; it supplies no complete-set enclosure and is never read by the final theorem gate.'''

[entries."inline-helper-heading-18"]
code_cell_ids = ["inline-helper-source-18", "producer-sampled-schur-diagnostics"]
chapter_labels = ["legEDMD:prop-validated-exact-binary-Schur-surrogate", "subsec:numerics-phase4-contours"]
evidence_status = ["empirical_diagnostic"]
mathematics = '''The retained diagnostic computes binary Schur factors and sampled contour profiles to test proposed packets.  In the validated theory, however, frozen factors must satisfy $R=A^\circ Q^\circ-Q^\circ T^\circ$ and $\delta_Q=\|(Q^\circ)^*Q^\circ-I\|_F<1$ before one may form $\widehat\kappa_Q=\sqrt{(1+\delta_Q)/(1-\delta_Q)}$ and the factorisation error $\eta_{\mathrm{Schur}}$.  This producer does not substitute sampled values for those rigorous checks.'''

[entries."inline-helper-heading-13"]
code_cell_ids = ["inline-helper-source-13", "a2b9f84258b4"]
chapter_labels = ["subsec:numerics-certification-provenance", "subsec:numerics-phase4-contours"]
evidence_status = ["validation_test", "presentation_only", "empirical_diagnostic"]
mathematics = '''The audit helper rebuilds retained diagnostic ladders and checks that their declared sources, shapes, and status transitions remain consistent.  Such a regression may detect accidental use of generated files or a diagnostic-to-certified upgrade.  It does not prove $\inf_{z\in\Gamma}\sigma_{\min}(zI-A_N^X)>0$ or compute a Riesz rank; it protects the logical plumbing around those later mathematical certificates.'''

[entries."42a2aee243d6"]
code_cell_ids = ["fd3908b317fe"]
chapter_labels = ["subsec:numerics-phase4-contours", "legEDMD:prop-certified-Blaschke-spectral-packets"]
evidence_status = ["presentation_only", "empirical_diagnostic"]
mathematics = '''The plotted circles $\Gamma_j=\{c_j+\delta_je^{i\theta}\}$ are candidate packet boundaries selected using external Blaschke target identities and finite-matrix reconnaissance.  Drawing a target point inside a circle neither proves the finite Schur count nor transfers that count to the exact operator.  The later validated chain independently counts Schur diagonal entries, certifies a complete moat, accounts for the silent zero block, and then checks small gain.'''

[entries."inline-helper-heading-4"]
code_cell_ids = ["inline-helper-source-4", "code-c57df58e"]
chapter_labels = ["legEDMD:prop-exact-dyadic-Hardy-matrix-replacement", "eq:numerics-Hardy-entry-error-radii", "eq:numerics-Hardy-row-column-radii", "eq:numerics-Hardy-matrix-replacement-radius-general", "eq:numerics-phase4-hardy-gauge-matrix"]
evidence_status = ["certified_finite_matrix"]
mathematics = '''Arb encloses the mathematical Hardy matrix $A_N^X$ and an exact-dyadic midpoint $A_N^\circ$.  If $e_{ij}\geq|(A_N^X)_{ij}-(A_N^\circ)_{ij}|$, then
$$
\eta_1=\max_j\sum_i e_{ij},\quad \eta_\infty=\max_i\sum_j e_{ij},\quad \eta_A=\sqrt{\eta_1\eta_\infty}
$$
gives $\|A_N^X-A_N^\circ\|_2\leq\eta_A$.  This turns entrywise balls into the operator-radius later subtracted from every midpoint moat.  A binary64 display copy is never a theorem input.'''

[entries."inline-helper-heading-5"]
code_cell_ids = ["inline-helper-source-5"]
chapter_labels = ["legEDMD:prop-validated-exact-binary-Schur-surrogate", "legEDMD:prop-complete-circle-triangular-Schur-moat", "legEDMD:prop-complete-circle-Laurent-moat", "legEDMD:cor-Schur-count-and-Hardy-moat-transport", "legEDMD:cor-certified-finite-to-exact-contour-rank"]
evidence_status = ["certified_spectral_transfer"]
mathematics = '''The exact similarity $S=Q^\circ T^\circ(Q^\circ)^{-1}$ preserves Schur diagonal counts, while $\widehat\kappa_Q$ controls coordinate distortion and $\eta_{\mathrm{Schur}}$ controls the failure of exact intertwining with $A_N^\circ$.  Seventeen complete circles use the triangular recurrence with $d_i=||t_{ii}-c|-\delta|$; seven use an exact-dyadic Laurent inverse with residual sum $\Delta_F<1$.  Counts remain Schur-derived.  The resulting moat is divided by $\widehat\kappa_Q$, then $\eta_{\mathrm{Schur}}$ and $\eta_A$ are subtracted under strict positivity gates.'''

[entries."bffa1d01"]
code_cell_ids = ["ffc45508", "code-b33b0f47", "3e8b784c", "128b5369"]
chapter_labels = ["eq:numerics-certified-lifted-moat-resolvent-bound", "eq:numerics-certified-small-gain", "legEDMD:prop-certified-Blaschke-spectral-packets", "legEDMD:cor-certified-exterior-Blaschke-spectrum"]
evidence_status = ["validation_test", "certified_spectral_transfer", "presentation_only"]
mathematics = '''The adversarial tests first verify that count, moat, replacement, silent-zero, and provenance gates fail independently.  Execution Cell 107N then uses
$$
\underline s_{\mathrm{lift}}=\min\{\underline s_{\Gamma,N}^{\mathrm H},d_{\Gamma,0}\},\qquad
\widehat\varepsilon/\underline s_{\mathrm{lift}}<1
$$
to transport each finite Schur count to the exact operator's Riesz rank.  All twenty-four counts are Schur-derived; Laurent witnesses certify moats only.  The external Blaschke formula subsequently identifies the packet names and exhausts the exterior tail, so that exterior classification is a hybrid theorem-plus-external conclusion.  Cells 108N and 109N are locked no-op terminals and add no claim.'''
